---
const { meta, title, description, class: className = '' } = Astro.props;
---

<article class:list={['card', className]}>
  {meta && <p class="card__meta">{meta}</p>}
  {title && <h3>{title}</h3>}
  {description && <p>{description}</p>}
  <slot />
</article>
